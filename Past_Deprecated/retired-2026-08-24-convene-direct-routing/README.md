# Retired direct-routing and state-bridge implementation

Retired on 2026-08-24 after Convene became the common source and result routing
plane. Nothing below this directory is an active deployment instruction.

This archive contains the former direct gateway-to-engine Cloudflare path,
Mac/Windows direct scenario publishers, the VM `sim_vars.json` state bridge,
quick-tunnel scripts, bridge-specific Convene bindings, and their historical
tests. They are preserved only for traceability and must not be copied to an
active source machine or cloud-engine release.

The current pickup point is
`deployment/CURRENT_CONVENE_ROUTED_SYSTEM_HANDOFF.md`.
